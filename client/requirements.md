## Packages
framer-motion | Required for complex entry animations on the widget
recharts | Required for the PieChart visualization in the widget

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  mono: ["var(--font-mono)"],
}
